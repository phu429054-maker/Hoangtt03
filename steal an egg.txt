local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local gui = Instance.new("ScreenGui")
gui.Name = "MaintenanceScreen"
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false
gui.DisplayOrder = 999999
gui.Parent = playerGui

local background = Instance.new("Frame")
background.Size = UDim2.fromScale(1, 1)
background.Position = UDim2.fromScale(0, 0)
background.BackgroundColor3 = Color3.fromRGB(18, 18, 20)
background.BorderSizePixel = 0
background.Parent = gui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.9, 0, 0, 80)
title.Position = UDim2.new(0.05, 0, 0.5, -75)
title.BackgroundTransparency = 1
title.Text = "The script has been deleted"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextScaled = true
title.TextWrapped = true
title.Parent = background

local line = Instance.new("Frame")
line.Size = UDim2.new(0.35, 0, 0, 2)
line.Position = UDim2.new(0.325, 0, 0.5, 10)
line.BackgroundColor3 = Color3.fromRGB(120, 120, 125)
line.BorderSizePixel = 0
line.Parent = background

local message = Instance.new("TextLabel")
message.Size = UDim2.new(0.85, 0, 0, 70)
message.Position = UDim2.new(0.075, 0, 0.5, 30)
message.BackgroundTransparency = 1
message.Text = "⚠ Script not found 404 ⚠"
message.TextColor3 = Color3.fromRGB(190, 190, 195)
message.Font = Enum.Font.Gotham
message.TextScaled = true
message.TextWrapped = true
message.Parent = background

local sub = Instance.new("TextLabel")
sub.Size = UDim2.new(0.8, 0, 0, 35)
sub.Position = UDim2.new(0.1, 0, 1, -55)
sub.BackgroundTransparency = 1
sub.Text = "HOANGTT.03 • MASSGER"
sub.TextColor3 = Color3.fromRGB(100, 100, 105)
sub.Font = Enum.Font.GothamMedium
sub.TextSize = 12
sub.Parent = background

title.TextTransparency = 1
message.TextTransparency = 1
line.BackgroundTransparency = 1

TweenService:Create(
    title,
    TweenInfo.new(0.6),
    {TextTransparency = 0}
):Play()

TweenService:Create(
    message,
    TweenInfo.new(0.8),
    {TextTransparency = 0}
):Play()

TweenService:Create(
    line,
    TweenInfo.new(0.7),
    {BackgroundTransparency = 0}
):Play()

task.delay(5, function()
    if gui and gui.Parent then
        gui:Destroy()
    end
end)
